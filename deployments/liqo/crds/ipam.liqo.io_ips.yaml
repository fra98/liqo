---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: ips.ipam.liqo.io
spec:
  group: ipam.liqo.io
  names:
    kind: IP
    listKind: IPList
    plural: ips
    singular: ip
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ip
      name: LocalIP
      type: string
    - jsonPath: .status.ip
      name: RemappedIP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IP is the Schema for the IP API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: IPSpec defines a local IP.
            properties:
              ip:
                description: IP is the local IP.
                type: string
            required:
            - ip
            type: object
          status:
            description: IPStatus defines a remapped IP.
            properties:
              ip:
                description: IP is the remapped IP returned by the IPAM module.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
